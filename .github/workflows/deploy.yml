name: Deploy to EasyPanel

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger EasyPanel Deploy
        run: |
          curl -X POST "https://hook.csvoa5.easypanel.host/api/deploy/9a523d29c85f9856196297cfba11939ac06722a408dafc86"

